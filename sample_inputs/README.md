# Sample Inputs

Sample source files used by `generate_sample_outputs.py` and referenced in the main [README](../README.md):

- `sample.csv` — 5 candidates (CSV source)
- `sample_ats.json` — Aisha Patel, structured ATS record (merges with her CSV row)
- `notes.txt` — Marcus Lee, free-text recruiter notes (merges with his CSV row)

Run `python generate_sample_outputs.py` from the project root to ingest these and produce `sample_outputs/`.
