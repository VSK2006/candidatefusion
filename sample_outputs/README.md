# Sample Outputs

Pre-generated by `generate_sample_outputs.py` from the files in `sample_inputs/`:

- `default_output.json` — all 5 candidates, full canonical schema
- `custom_config_output.json` — field-selected and renamed output via the Required Twist projection layer
- `skills_only_output.json` — skills-only projection (`on_missing: omit`)

See the main [README](../README.md) for the `OutputConfig` format used to produce the custom outputs.
